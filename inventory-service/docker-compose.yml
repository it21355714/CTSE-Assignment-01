version: '3.8'  

services:
  mongo:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
    networks:
      - inventory-net

  iinventory-service:
    build:
      context: ..
      dockerfile: ./Dockerfile
    container_name: inventory-service
    ports:
      - "8085:8080"
    environment:
      SPRING_DATA_MONGODB_URI: mongodb+srv://tharindunimedicode455:HZ3JO1rUQCFAU26a@ctse-cluster.acuuqvz.mongodb.net/inventory_service?retryWrites=true&w=majority&appName=CTSE-Cluster

    depends_on:
      - mongo
    networks:
      - inventory-net

networks:
  inventory-net: